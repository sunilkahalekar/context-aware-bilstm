# Curated Figures

Empty by design for now — this is where to place the final, paper-ready
figure selections once they're chosen, so they don't have to be hunted for
across the three `src/analysis/*/figures/` folders. Until curated, the
authoritative source for every figure is still its originating analysis
folder (see the root `README.md`'s pipeline map for which stage produced
which chart).
